{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{{ '{% block title %}{{ "crmp.create"|trans }}{% endblock %}' }}

{% block body %}
{{ "{% block body %}" }}
    <h1>{{ entity }}</h1>

    {{ '{{ form_start(form) }}' }}
        {{ '{{ form_widget(form) }}' }}
        <button type="submit" class="btn btn-primary">
            <i class="fa fa-floppy-o"></i>
            {{ "crmp.create"|trans }}
        </button>
    {{ '{{ form_end(form) }}' }}

{% set hide_edit, hide_delete = true, true %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
