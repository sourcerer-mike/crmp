<?xml version="1.0"?>
<ruleset name="CRMP Standard">

    <description>Derived Symfony standard for the CRMP project</description>

    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/data/*</exclude-pattern>

    <!--
       Include all sniffs in the Symfony standard.
    -->
    <rule ref="Symfony2"/>
</ruleset>